{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Выбор источника и открытие готовой папки в файловом менеджере",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "opener:allow-reveal-item-in-dir"
  ]
}
